extends ../layout 

block content 
    h1= title
    if  books.length
        ul
            each book in books
                li 
                    a(href=book.url) #{book.title}
                    |   (#{book.author.getFullName})
    else 
        p #{t('books.not_exist')}
